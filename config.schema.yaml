---
api_key:
  description: "The API key for OpsGenie."
  type: "string"
  secret: true
  required: true
region:
  description: "The service region for your OpsGenie account"
  type: "string"
  enum:
    - "eu"
    - "us"
  default: "us"
