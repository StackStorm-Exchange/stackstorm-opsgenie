---
description: "Disable an OpsGenie Policy"
enabled: true
entry_point: "disable_policy.py"
name: "disable_policy.yaml"
pack: "opsgenie"

runner_type: "python-script"

parameters:
  policy_id:
    type: "string"
    description: "Policy to Disable"
    required: true
  team_id:
    type: "string"
    description: " The identifier of the team that this policy belongs."
    default: ~
