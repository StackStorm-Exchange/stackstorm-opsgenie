---
description: "Enable an OpsGenie Policy"
enabled: true
entry_point: "enable_policy.py"
name: "enable_policy.yaml"
pack: "opsgenie"

runner_type: "python-script"

parameters:
  policy_id:
    type: "string"
    description: "Policy to Enable"
    required: true
  team_id:
    type: "string"
    description: " The identifier of the team that this policy belongs."
    default: ~
